#! /bin/sh
#
# this is shell script to perform silent installation of Oracle DB
# during puppet deployment and make the process
# omnipotent
#
#
# this can't be run as root, but we don't do it anyway as we use user 'deploy'

<%= @installation_dir %>/database/runInstaller -silent -noconfig -ignoreSysprereqs -ignorePrereq -waitforcompletion -responseFile /etc/defra_response_file.rsp

# below is to set "a flag" so that install command runs only once
touch /tmp/oracleIsInstalled
# after successful run switch to root user
# not yet complete