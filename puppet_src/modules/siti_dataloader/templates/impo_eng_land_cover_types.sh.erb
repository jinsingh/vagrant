#!/bin/bash

set -xe

export LD_LIBRARY_PATH="/usr/lib/oracle/12.1/client64/lib/:/opt/capd-siti-linux-dataloader/"

./csv2ora -d "//<%= scope.function_hiera(['caporacledb-ip-host']) %>:<%= scope.function_hiera(["oracle-port"]) %>/<%= scope.function_hiera(['capd-oracle-sid']) %>" -u <%= scope.function_hiera(['capd-oracle-username']) %> -p <%= scope.function_hiera(['capd-oracle-capduser-password']) %> -t load_land_cover_types -l load_land_cover_types.log -f /tmp/load_land_cover_types.txt -c load_land_cover_types.cfg

