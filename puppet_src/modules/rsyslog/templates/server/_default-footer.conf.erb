#Slight hack, will work well to ensure we can do searches
#of logs coming from remote hosts and send emails based on
#those searches.  If this is not included here only local syslog
#entries are searched.
$IncludeConfig /etc/rsyslog.d/alertemail.conf

# Switch back to default ruleset
$RuleSet RSYSLOG_DefaultRuleset

<% if scope.lookupvar('rsyslog::server::enable_udp') -%>
$InputUDPServerBindRuleset remote
$UDPServerRun 514
<% end -%>

<% if scope.lookupvar('rsyslog::server::enable_tcp') -%>
$InputTCPServerBindRuleset remote
$InputTCPServerRun 514
<% end -%>
